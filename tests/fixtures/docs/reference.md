# The parts format

`parts.json` holds the parts widget reads, and a reader looks it up when writing one.

## Fields

| Field | What it holds |
| --- | --- |
| `name` | the part's name |
| `supplier` | who sells it |
